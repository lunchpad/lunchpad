<%= form_for :off_day do |f| %>
  <%= f.label 'Off Day Description' %>
  <%= f.text_field :name %>
  <%= f.label 'Date (select one or multiple dates with the same description)' %>
  <%= f.text_field :date, id: 'datepicker' %>
  <%= f.submit 'Submit' %>
<% end %>