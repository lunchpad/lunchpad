<div class="order_form_wrapper">
  <div class="order_form_title">
    Edit Order - Week of <%= @order.begin_date.strftime("%B %d, %Y") %>
  </div>
</div>

<%= render partial: 'form', locals: {account: @account, order: @order, ordered_items: @ordered_items} %>